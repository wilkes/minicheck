<project name="minicheck" default="compile">
  <description>
    A QuickCheck in Clojure
  </description>

  <exec executable="bin/version" outputproperty="version" />
  <property name="src.clj" location="src"/>
  <property name="build" location="build"/>
  <property name="dist" location="dist"/>
  <property name="lib" value="lib" />
  <property name="jar" location="${dist}/minicheck-${version}.jar"/>

  <target name="clean"
          description="Remove autogenerated files and directories">
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>

  <target name="init">
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init,resolve"
          description="Compile source">
    <java classname="clojure.lang.Compile">
      <classpath>
          <pathelement path="${src.clj}"/>
          <pathelement path="${build}"/>
          <fileset dir="${lib}">
            <include name="**/*.jar"/>
          </fileset>
      </classpath>
      <sysproperty key="clojure.compile.path" value="${build}"/>
    </java>
  </target>

  <target name="dist" depends="compile" description="Create jar for distribution">
    <mkdir dir="${dist}"/>
    <jar jarfile="${jar}" basedir="${build}">
      <fileset dir="${src.clj}">
        <include name="**/*.clj"/>
      </fileset>
      <manifest>
        <attribute name="Class-Path" value="."/>
      </manifest>
    </jar>
  </target>
</project>
